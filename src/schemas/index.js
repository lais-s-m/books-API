import { createUserSchema } from "./user.schema";
import { createBookSchema } from "./book.schema";
import { loginSchema } from "./login.schema";

export { createUserSchema, createBookSchema, loginSchema };
